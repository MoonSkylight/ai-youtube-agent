import { NextResponse } from "next/server";
import { requireUser } from "@/lib/auth";
import { supabaseAdmin } from "@/lib/db/server";
export async function GET(_request: Request, context: { params: Promise<{ id: string }> }) { try { const user = await requireUser(); const { id } = await context.params; const { data: asset, error } = await supabaseAdmin.from("media_assets").select("id, user_id, storage_path").eq("id", id).eq("user_id", user.id).single(); if (error || !asset) return NextResponse.json({ ok:false, error:"Asset not found" }, { status:404 }); const { data: signed, error: signedError } = await supabaseAdmin.storage.from("media-assets").createSignedUrl(asset.storage_path, 60*10); if (signedError || !signed) return NextResponse.json({ ok:false, error:signedError?.message || "Failed to create signed URL" }, { status:500 }); return NextResponse.json({ ok:true, url:signed.signedUrl }); } catch (error) { console.error(error); return NextResponse.json({ ok:false, error:"Failed to load asset URL" }, { status:500 }); } }
