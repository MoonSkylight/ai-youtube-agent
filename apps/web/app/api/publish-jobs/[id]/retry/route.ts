import { NextResponse } from "next/server";
import { requireUser } from "@/lib/auth";
import { supabaseAdmin } from "@/lib/db/server";
export async function POST(_request: Request, context: { params: Promise<{ id: string }> }) { try { const user = await requireUser(); const { id } = await context.params; const { data: job, error } = await supabaseAdmin.from("publish_jobs").select("id, user_id, attempts, status").eq("id", id).eq("user_id", user.id).single(); if (error || !job) return NextResponse.json({ ok:false, error:"Publish job not found" }, { status:404 }); const { data, error: updateError } = await supabaseAdmin.from("publish_jobs").update({ status: "queued", failure_reason: null, attempts: (job.attempts ?? 0) + 1, updated_at: new Date().toISOString() }).eq("id", id).eq("user_id", user.id).select("*").single(); if (updateError) return NextResponse.json({ ok:false, error:updateError.message }, { status:500 }); return NextResponse.json({ ok:true, publishJob:data }); } catch (error) { console.error(error); return NextResponse.json({ ok:false, error:"Failed to retry publish job" }, { status:500 }); } }
