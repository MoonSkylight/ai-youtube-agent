import { NextResponse } from "next/server";
export async function GET(request: Request) { const url = new URL(request.url); const origin = url.origin; const redirectUri = `${origin}/api/integrations/oauth/callback/tiktok`; const state = crypto.randomUUID(); const clientId = process.env.TIKTOK_CLIENT_ID; if (!clientId) return NextResponse.redirect(new URL("/integrations?error=tiktok_client_id_missing", request.url)); const authUrl = new URL("https://www.tiktok.com/v2/auth/authorize/"); authUrl.searchParams.set("client_key", clientId); authUrl.searchParams.set("redirect_uri", redirectUri); authUrl.searchParams.set("response_type", "code"); authUrl.searchParams.set("scope", "video.publish"); authUrl.searchParams.set("state", state); const response = NextResponse.redirect(authUrl.toString()); response.cookies.set("oauth_state_tiktok", state, { httpOnly: true, sameSite: "lax", path: "/" }); return response; }
