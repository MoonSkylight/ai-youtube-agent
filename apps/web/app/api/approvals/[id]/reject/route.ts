import { NextRequest, NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/db/server";
import { requireUser } from "@/lib/auth";
export async function POST(_request: NextRequest, context: { params: Promise<{ id: string }> }) { try { const user = await requireUser(); const { id } = await context.params; const { data: approval, error: findError } = await supabaseAdmin.from("approvals").select("id, user_id, status").eq("id", id).eq("user_id", user.id).single(); if (findError || !approval) return NextResponse.json({ ok:false, error:"Approval not found" }, { status:404 }); if (approval.status !== "pending") return NextResponse.json({ ok:false, error:"Approval is not pending" }, { status:400 }); const { error: updateError } = await supabaseAdmin.from("approvals").update({ status: "rejected", rejected_at: new Date().toISOString() }).eq("id", id).eq("user_id", user.id); if (updateError) return NextResponse.json({ ok:false, error:updateError.message }, { status:500 }); return NextResponse.json({ ok:true, rejected:true }); } catch (error) { console.error(error); return NextResponse.json({ ok:false, error:"Failed to reject action" }, { status:500 }); } }
