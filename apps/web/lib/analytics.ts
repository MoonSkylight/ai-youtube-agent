import { requireUser } from "@/lib/auth"; import { supabaseAdmin } from "@/lib/db/server"; import { AnalyticsSummary } from "@/lib/types";
export async function getAnalyticsSummary(): Promise<AnalyticsSummary> { const user = await requireUser(); const [openTasks,pendingApprovals,listingDrafts,scriptDrafts,queuedPublishJobs,postedPublishJobs,appProjects] = await Promise.all([supabaseAdmin.from("tasks").select("id", { count: "exact", head: true }).eq("user_id", user.id).neq("status", "completed"), supabaseAdmin.from("approvals").select("id", { count: "exact", head: true }).eq("user_id", user.id).eq("status", "pending"), supabaseAdmin.from("listings").select("id", { count: "exact", head: true }).eq("user_id", user.id).eq("status", "draft"), supabaseAdmin.from("scripts").select("id", { count: "exact", head: true }).eq("user_id", user.id).eq("status", "draft"), supabaseAdmin.from("publish_jobs").select("id", { count: "exact", head: true }).eq("user_id", user.id).eq("status", "queued"), supabaseAdmin.from("publish_jobs").select("id", { count: "exact", head: true }).eq("user_id", user.id).eq("status", "posted"), supabaseAdmin.from("app_projects").select("id", { count: "exact", head: true }).eq("user_id", user.id)]); return { openTasks: openTasks.count ?? 0, pendingApprovals: pendingApprovals.count ?? 0, listingsDrafts: listingDrafts.count ?? 0, scriptsDrafts: scriptDrafts.count ?? 0, queuedPublishJobs: queuedPublishJobs.count ?? 0, postedPublishJobs: postedPublishJobs.count ?? 0, appProjects: appProjects.count ?? 0 }; }
