"use client";
import { useRouter, useSearchParams } from "next/navigation";
export function EventLogFilters() { const router = useRouter(); const searchParams = useSearchParams(); const status = searchParams.get("status") || ""; const eventType = searchParams.get("eventType") || ""; function updateParam(key: string, value: string) { const params = new URLSearchParams(searchParams.toString()); if (value) params.set(key, value); else params.delete(key); router.push(`/workers?${params.toString()}`); } return <div className="flex flex-wrap gap-3"><select value={status} onChange={(e) => updateParam("status", e.target.value)} className="rounded-xl border px-3 py-2 text-sm"><option value="">All statuses</option><option value="info">Info</option><option value="warning">Warning</option><option value="error">Error</option></select><input value={eventType} onChange={(e) => updateParam("eventType", e.target.value)} placeholder="Filter event type" className="rounded-xl border px-3 py-2 text-sm" /></div>; }
