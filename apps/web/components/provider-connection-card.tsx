import { StatusBadge } from "@/components/status-badge";
export function ProviderConnectionCard({ provider, accountLabel, externalAccountId, scope, updatedAt, isConnected }: { provider: string; accountLabel?: string | null; externalAccountId?: string | null; scope?: string | null; updatedAt?: string | null; isConnected: boolean; }) { return <div className="rounded-xl border p-4"><div className="flex items-center justify-between gap-4"><div><div className="font-medium capitalize">{provider}</div><div className="text-sm text-neutral-500">{accountLabel || "No label"}</div>{externalAccountId ? <div className="text-xs text-neutral-500">Account: {externalAccountId}</div> : null}{scope ? <div className="text-xs text-neutral-500">Scope: {scope}</div> : null}{updatedAt ? <div className="text-xs text-neutral-500">Updated: {updatedAt}</div> : null}</div><StatusBadge status={isConnected ? "approved" : "draft"} /></div></div>; }
