"use client"; import { useState } from "react";
export function ApprovalActions({ approvalId }: { approvalId: string }) { const [loading,setLoading]=useState(false); async function handleAction(action:"approve"|"reject"){ setLoading(true); try{ await fetch(`/api/approvals/${approvalId}/${action}`, { method:"POST" }); window.location.reload(); } finally { setLoading(false); } } return <div className="flex gap-2"><button className="rounded-xl border px-4 py-2" disabled={loading} onClick={()=>handleAction("approve")}>Approve</button><button className="rounded-xl border px-4 py-2" disabled={loading} onClick={()=>handleAction("reject")}>Reject</button></div>; }
