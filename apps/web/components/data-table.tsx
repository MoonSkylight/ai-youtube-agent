type Column<T> = { key: keyof T; label: string; render?: (value: T[keyof T], row: T) => React.ReactNode; };
export function DataTable<T extends Record<string, unknown>>({ title, rows, columns }: { title: string; rows: T[]; columns: Column<T>[]; }) { return <div className="rounded-2xl border p-5 shadow-sm"><h3 className="text-lg font-semibold">{title}</h3><div className="mt-4 overflow-x-auto"><table className="min-w-full text-sm"><thead><tr className="border-b">{columns.map((column)=><th key={String(column.key)} className="px-3 py-2 text-left font-medium text-neutral-500">{column.label}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={columns.length} className="px-3 py-4 text-neutral-500">No data yet.</td></tr>:rows.map((row,index)=><tr key={index} className="border-b last:border-b-0">{columns.map((column)=><td key={String(column.key)} className="px-3 py-3 align-top">{column.render ? column.render(row[column.key], row) : String(row[column.key] ?? "")}</td>)}</tr>)}</tbody></table></div></div>; }
