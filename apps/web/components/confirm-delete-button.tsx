"use client"; import { useState } from "react";
export function ConfirmDeleteButton({ endpoint, label = "Delete", confirmText = "Are you sure?" }: { endpoint: string; label?: string; confirmText?: string }) { const [loading,setLoading]=useState(false); async function handleDelete(){ const ok=window.confirm(confirmText); if(!ok) return; setLoading(true); try{ await fetch(endpoint, { method:"POST" }); window.location.reload(); } finally { setLoading(false); } } return <button disabled={loading} onClick={handleDelete} className="rounded-xl border px-3 py-2 text-sm">{loading ? "Deleting..." : label}</button>; }
